- ## create a method to handel all requests 
    - **then create seprate methods to handel each type of request: for example, get(), post(), delete()**

- ## Get acategores from each api, filter them and store them in a single object;
